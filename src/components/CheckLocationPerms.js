import React, { Component } from 'react';
import getLocation from '../helpers/getLocation';

class CheckLocationPerms extends Component {
  render() {
    return (
      <CheckLocationPerms>
      </CheckLocationPerms>
    )
  }
};

export default CheckLocationPerms;